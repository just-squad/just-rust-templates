# just-rust-templates

Шаблоны проектов на Rust от [just-squad](https://github.com/just-squad) для быстрого старта.  
Сейчас включает:

- 🤖 **Telegram-бот** — готовый шаблон с асинхронной обработкой сообщений.
- 🌐 **Web API** — минималистичный HTTP-сервер на `axum` с логированием и обработкой ошибок.

Идеально подходит, чтобы не копировать boilerplate вручную и сразу начать писать логику.

---

## 🔧 Требования

- [Rust](https://www.rust-lang.org/tools/install) (рекомендуется использовать `rustup`)
- [`cargo-generate`](https://github.com/cargo-generate/cargo-generate)

### Установка cargo-generate

```sh
cargo install cargo-generate
```

> **Примечание**: Если впервые используете `cargo install`, убедитесь, что `~/.cargo/bin` добавлен в `PATH`.

---

## 🚀 Как использовать

1. Выберите нужный шаблон из списка выше.
2. Запустите генерацию проекта:

   ```sh
   cargo generate --git https://github.com/just-squad/just-rust-templates --name my-project-name
   ```

   Утилита предложит выбрать один из доступных шаблонов.

3. Перейдите в созданную папку и запустите:

   ```sh
   cd my-project-name
   cargo run
   ```

Готово! Теперь у вас есть рабочий проект на Rust по выбранному шаблону.

---

## 🛠️ Добавить свой шаблон?

Приветствуются PR! Просто добавьте новую папку с шаблоном в корень репозитория и обновите `README`.

---

Лицензия: [MIT](./LICENSE)
